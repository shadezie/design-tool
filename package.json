{
  "name": "design-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for design QA. The extension itself has no build step and no runtime dependencies; this file exists only for the end-to-end tests.",
  "type": "module",
  "scripts": {
    "test": "node test/verify.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
